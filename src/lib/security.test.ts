import { describe,expect,it } from "vitest";import { safeEqual,sanitizeFileName,sha256 } from "./security";describe("security helpers",()=>{it("sanitizes paths",()=>expect(sanitizeFileName("../../ảnh sản phẩm.png")).not.toContain("/"));it("compares secrets",()=>{expect(safeEqual("secret-123","secret-123")).toBe(true);expect(safeEqual("a","b")).toBe(false)});it("hashes deterministically",()=>expect(sha256("a")).toBe(sha256("a")))})
